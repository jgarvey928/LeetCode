name: Auto-Update README

on:
  push:
    branches:
      - main
    # I removed the 'paths' rule here! Now it runs on EVERY push.

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Run script to completely overwrite README
        run: python generate_readme.py

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          
          # If the python script changed the README, save it and force push it!
          git diff --quiet && git diff --staged --quiet || git commit -m "docs: auto-update README table and clean up extension"
          
          git pull origin main --rebase
          git push origin main
